info:
  name: User Login
  type: http
  seq: 2

http:
  method: POST
  url: "{{BaseUrl}}/api/auth/login"
  body:
    type: json
    data: "{\r

      \  \"email\": \"prahadeesh4@gmail.com\",\r

      \  \"password\": \"password\"\r

      }"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
